# Pin Robolectric to compileSdk (34) so the simulated Android runtime is
# reproducible across machines/CI regardless of the latest SDK Robolectric ships.
# Prefer this central file over per-class @Config(sdk = ...).
sdk=34
